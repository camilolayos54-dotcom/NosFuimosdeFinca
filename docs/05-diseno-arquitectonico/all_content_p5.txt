# Deliverable 1 (D1): Quality Attributes Map

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

### 1. Utility Tree

Este Ã¡rbol mapea y prioriza los Atributos de Calidad crÃ­ticos (basados en los NFRs definidos en Fase 3/4) que la arquitectura DEBE resolver.

| Quality Attribute | Refinement / Focus | Business Value | Architectural Impact |
| --- | --- | --- | --- |
| **Integridad de Datos** | Tolerancia Cero a Reservas Dobles (*Double Booking*) bajo concurrencia. | Alta | Alta |
| **Disponibilidad** | Tolerancia a fallos en la red durante la recepciÃ³n del Webhook de Pagos. | Alta | Alta |
| **Rendimiento** | BÃºsqueda y filtrado de Fincas ultrarrÃ¡pido (Read-Heavy). | Alta | Media |
| **Seguridad** | Aislamiento de datos por Propietario (*Row-Level Security*). | Alta | Alta |
| **Mantenibilidad** | Facilidad de Onboarding para desarrolladores y modularidad. | Media | Media |

### 2. Concrete Architectural Scenarios (SEI Format)

#### Scenario 1: Prevenir *Double Booking* bajo alta concurrencia
- **Source:** Dos turistas (MÃºltiples clientes simultÃ¡neos).
- **Stimulus:** Intentan iniciar un pago para las mismas fechas de la misma finca en el mismo milisegundo (M-03).
- **Environment:** Temporada alta (Black Friday / Semana Santa).
- **Artifact:** MÃ³dulo de Reservas y Base de Datos.
- **Response:** El sistema otorga un *Soft-Lock* al primero que entra y rechaza (HTTP 409) al segundo de manera atÃ³mica, sin ensuciar la base de datos.
- **Measure:** GarantÃ­a matemÃ¡tica de 0% colisiones (ACID). ResoluciÃ³n en < 500ms.

#### Scenario 2: Resiliencia del Webhook de Pagos
- **Source:** Pasarela Externa (Wompi).
- **Stimulus:** EnvÃ­a el webhook confirmando el cobro de $200 (M-04).
- **Environment:** OperaciÃ³n normal, pero el hilo de Node/Java estÃ¡ bloqueado o la BD se estÃ¡ reiniciando.
- **Artifact:** Endpoint HTTP de RecepciÃ³n y Procesador asÃ­ncrono.
- **Response:** El sistema debe capturar el payload inmediatamente, encolarlo para procesarlo de forma resiliente y responder 200 OK rÃ¡pido para que Wompi no re-intente infinitamente.
- **Measure:** Responder a Wompi en < 1 segundo. Idempotencia del 100%.

#### Scenario 3: Rendimiento en el Motor de BÃºsqueda
- **Source:** Turista anÃ³nimo.
- **Stimulus:** Carga la pÃ¡gina principal o filtra por "Fincas en el QuindÃ­o con Piscina" (M-02).
- **Environment:** TrÃ¡fico pico de domingos en la noche.
- **Artifact:** MÃ³dulo de BÃºsqueda (API GET /fincas).
- **Response:** El sistema retorna la lista de fincas aprobadas con sus imÃ¡genes pre-optimizadas.
- **Measure:** Latencia en el P95 menor a 300ms.
# Deliverable 10 (D10): ADR Consolidation

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

### Architecture Decision Log

Este documento indexa las decisiones tÃ©cnicas irrevocables tomadas durante la Fase 5. NingÃºn desarrollador puede contravenir estas reglas durante las Fases 6 y 7 sin escalar un rediseÃ±o de la arquitectura.

| ADR ID | Decision Area | Selected Approach | Key Justification |
| --- | --- | --- | --- |
| **ADR-001** | Deployment Topology | HÃ­brido: **Vercel (Front) + AWS (Back y DB)**. | Vercel maximiza el performance de SSR (Next.js), mientras AWS provee la infraestructura madura para base de datos relacional (RDS) y escalabilidad del backend (Beanstalk/ECS). |
| **ADR-002** | System Decomposition | **Modular Monolith** en Spring Boot. | Reduce latencia inter-mÃ³dulo (0ms en llamadas a memoria) y simplifica drÃ¡sticamente el manejo de transacciones ACID y Rollbacks frente a una arquitectura de Microservicios. |
| **ADR-003** | Architectural Style | Externo: **REST API** (JSON). Interno: **Event-Driven** (ApplicationEvents). | REST garantiza mÃ¡xima compatibilidad con cualquier cliente B2C. Eventos internos desacoplan mÃ³dulos evitando cuellos de botella por notificaciones y correos. |
| **ADR-004** | Communication Pattern | **SÃ­ncrono para lecturas/escrituras primarias. AsÃ­ncrono vÃ­a Webhooks y Tareas Background para 3eros.** | El backend debe aceptar de Wompi la orden, encolar el correo asÃ­ncronamente y devolver HTTP 200 rÃ¡pido (Idempotencia). |
# Deliverable 11 (D11): Phase 5 RTM Update & Brief

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

### 2. Traceability Matrix Update

La siguiente matriz amarra los requerimientos funcionales crÃ­ticos de la Fase 3 y los mÃ³dulos UI de la Fase 4 con sus respectivos contenedores arquitectÃ³nicos definidos en el D9.

| Req ID / Module | Description | Phase 5 Architectural Container |
| --- | --- | --- |
| **M-01 / AUTH** | Registro e Inicio de sesiÃ³n B2C/B2B. | `API Monolithic Backend` (Capa de Seguridad Spring Security) |
| **M-02 / SRCH** | BÃºsqueda pÃºblica y filtrado de Fincas. | `API Monolithic Backend` (Capa de CatÃ¡logo) -> `Relational Database` |
| **M-03 / RSV** | Motor transaccional de bloqueos (Soft/Hard Lock). | `Relational Database` (PostgreSQL `SELECT FOR UPDATE`) |
| **M-04 / PAY** | Pasarela de Pago Wompi y Webhook. | `Wompi` (Externo) -> `API Monolithic Backend` (Background Worker) |
| **M-08 / FCNT** | Subida de fotos de la finca. | `Single Page App` -> `API Monolithic Backend` -> `Amazon S3` |
| **M-10 / NOT** | EnvÃ­o de correos asÃ­ncronos. | `API Monolithic Backend` (Event Listener) -> `SendGrid` |

*Zero Gaps Detectados:* Todas las funcionalidades principales poseen un contenedor de CÃ³mputo, Almacenamiento o IntegraciÃ³n Externa asignado, garantizando que el diseÃ±o arquitectÃ³nico soporta el 100% de los casos de uso.

### 3. Phase 5 Execution Brief

Durante la Fase 5 se tomaron y cimentaron las decisiones de infraestructura clave bajo las que operarÃ¡ *Nos Fuimos de Finca*. Optamos por una topologÃ­a Cloud HÃ­brida (Vercel para el Frontend y AWS para el Backend y Persistencia), asegurando el mejor rendimiento de su clase para ambas tecnologÃ­as. A nivel sistÃ©mico, elegimos un **Monolito Modular Estricto** para simplificar el pipeline de CI/CD y garantizar transacciones ACID atÃ³micas en la reserva, evitando la sobrecarga en red de los microservicios sin sacrificar el orden lÃ³gico.

El modelado C4 (Contexto y Contenedores) ya es preciso y ha sido verificado contra los NFRs. Por lo tanto, declaro el cierre formal de la Fase 5 y la **disposiciÃ³n total del proyecto para avanzar a la Fase 6 (Technical Design)**, donde estas decisiones de alto nivel se traducirÃ¡n en la estructura real de carpetas, cÃ³digo y esquemas SQL.
# Deliverable 2 (D2): Bounded Context Formalization

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

### 2. Context Definitions

| Bounded Context | Classification | Responsibility | Owned Domain Entities |
| --- | --- | --- | --- |
| **Booking & Payment** | Core | GestiÃ³n del ciclo de vida de la reserva, bloqueos de concurrencia (Soft-Lock/Hard-Lock) y conciliaciÃ³n financiera. | `Reserva`, `Pago` |
| **Catalog & Content** | Core | Descubrimiento de fincas, motor de bÃºsqueda pÃºblico y calificaciones post-viaje. | `Finca`, `Amenidad`, `ReseÃ±a` |
| **Identity & Access** | Generic | AutenticaciÃ³n, control de acceso (JWT) y perfiles de usuario. | `Usuario` |
| **Notifications** | Supporting | EnvÃ­o de correos asÃ­ncronos y alertas del sistema. | N/A (Operacional) |

### 3. Context Map Diagram

```mermaid
flowchart TD
    %% Contexts
    C_ID[Identity & Access Context]
    C_CAT[Catalog & Content Context]
    C_BOOK[Booking & Payment Context]
    C_NOT[Notifications Context]
    
    %% Relationships
    C_ID -- "Shared Kernel (JWT/Claims)" --> C_CAT
    C_ID -- "Shared Kernel (JWT/Claims)" --> C_BOOK
    
    C_CAT -- "Customer/Supplier (Upstream: Catalog)" --> C_BOOK
    
    C_BOOK -- "Publisher/Subscriber (Async Events)" --> C_NOT
    C_CAT -- "Publisher/Subscriber (Async Events)" --> C_NOT
    C_ID -- "Publisher/Subscriber (Async Events)" --> C_NOT
```
# Deliverable 3 (D3): Deployment Topology Decision

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

### 2. Architectural Decision Record (ADR)

**ADR-005: Cloud Deployment Topology**

- **Context:** Necesitamos definir dÃ³nde hospedar la plataforma PolÃ­glota (Next.js + Spring Boot) para cumplir con los NFRs de escalabilidad y alta disponibilidad estipulados en D1, mientras mantenemos un "Time-to-Market" rÃ¡pido.
- **Options Considered:** 
  1. Kubernetes Nativo (EKS) para todo el ecosistema.
  2. Plataforma HÃ­brida: Vercel (Front) + AWS Managed Services (Back).
  3. Todo en Vercel (Next.js Serverless Backend).
- **Decision:** Se elige la OpciÃ³n 2 (**Plataforma HÃ­brida: Vercel + AWS**). El frontend residirÃ¡ en Vercel Edge Network. El Backend (Monolito Spring Boot) correrÃ¡ en AWS Elastic Beanstalk (o ECS), anclado a un Amazon RDS PostgreSQL gestionado.
- **Consequences:** 
  - *Positive:* Next.js rinde a velocidad mÃ¡xima en Vercel (SSR y Edge Caching). RDS PostgreSQL garantiza la integridad ACID para evitar Double Bookings sin lidiar con parches de SO. 
  - *Negative:* Introduce latencia de red (aprox 20-30ms) entre Vercel y el VPC de AWS, requiriendo encriptaciÃ³n TLS en trÃ¡nsito obligatoria.

### 3. Network/Deployment Diagram

```mermaid
graph TD
    User((Usuario/Navegador))
    
    subgraph "Vercel Edge Network (Frontend)"
        CDN[Edge CDN]
        NextJS[Next.js App (SSR)]
    end
    
    subgraph "AWS Cloud (Backend & Data)"
        ALB[Application Load Balancer]
        
        subgraph "Auto Scaling Group"
            SpringBoot1[Spring Boot App Node 1]
            SpringBoot2[Spring Boot App Node 2]
        end
        
        RDS[(Amazon RDS PostgreSQL)]
        S3[(Amazon S3 - Fotos)]
    end
    
    User -- HTTPS --> CDN
    CDN -- Edge Routing --> NextJS
    
    NextJS -- HTTPS (API REST) --> ALB
    ALB -- HTTP --> SpringBoot1
    ALB -- HTTP --> SpringBoot2
    
    SpringBoot1 -- JDBC/SSL --> RDS
    SpringBoot2 -- JDBC/SSL --> RDS
    
    SpringBoot1 -- AWS SDK --> S3
    SpringBoot2 -- AWS SDK --> S3
```
# Deliverable 4 (D4): System Decomposition Decision

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

## Architecture Decision Record (ADR-002)

### Context
Existen 3 Contextos Limitados principales (Transaccional, CatÃ¡logo e Identidad). Con las arquitecturas modernas, es tentador crear un microservicio para cada contexto (e.g. un servidor Node para Auth, un servidor Java para Reservas, etc.). Sin embargo, la escala operativa del proyecto actualmente es de un equipo tÃ©cnico pequeÃ±o iniciando operaciones ("Startup Phase").

### Decision
Se adoptarÃ¡ una arquitectura de **Monolito Modular Estricto (Modular Monolith)** para el Backend.
Todos los contextos limitados (Auth, Fincas, Reservas) residirÃ¡n dentro del **mismo** repositorio de cÃ³digo y se compilarÃ¡n en un **Ãºnico artefacto ejecutable** (.jar / Docker Image) administrado por Spring Boot.

Se prohÃ­be explÃ­citamente el aislamiento fÃ­sico (Microservicios), pero se exige aislamiento lÃ³gico: los paquetes internos (`com.finca.booking`, `com.finca.catalog`) no deben cruzar lÃ­mites transaccionales ni inyectar repositorios de otros contextos, deben comunicarse mediante interfaces o eventos de dominio en memoria (ApplicationEvents).

### Consequences
**Positivas:**
- **Zero Network Overhead:** Las llamadas entre el motor de bÃºsqueda y las reservas se hacen en memoria (0ms), cumpliendo fÃ¡cilmente con los NFRs de rendimiento.
- **Transaccionalidad Simple:** Es posible manejar transacciones de BD nativas complejas y hacer `Rollback` de todo sin recurrir a patrones complejos como *Saga Pattern* o *Two-Phase Commits*.
- **Despliegue unificado:** CI/CD drÃ¡sticamente mÃ¡s simple. Una sola base de datos (Un esquema, mÃºltiples tablas).

**Negativas / Riesgos:**
- El cÃ³digo puede convertirse en un "Big Ball of Mud" si no se respetan las interfaces internas (Se mitigarÃ¡ con reglas estrictas de empaquetado como ArchUnit en Java o validadores de dependencias).
- Si el mÃ³dulo de bÃºsqueda sufre un pico de trÃ¡fico masivo (DDOS), todo el monolito puede caer, afectando al mÃ³dulo transaccional. (Se mitigarÃ¡ escalando horizontalmente *todo* el monolito tras un Load Balancer de AWS).
# Deliverable 5 (D5): Architectural Style Selection

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

## Architecture Decision Record (ADR-003)

### Context
El Frontend (Next.js) y el App MÃ³vil (Futura) necesitan comunicarse con el Backend MonolÃ­tico, y los propios mÃ³dulos internos del monolito necesitan comunicarse entre sÃ­ tras completar acciones crÃ­ticas (ej. enviar correos despuÃ©s de reservar).

### Decision
1. **Externo (Client-to-Server):** El contrato hacia el exterior serÃ¡ un **API RESTful**.
   - GraphQL se descarta por aÃ±adir sobrecarga cognitiva no requerida para modelos relacionales planos.
   - gRPC se descarta porque el cliente principal es un navegador web (JSON es el estÃ¡ndar).
   - Se aplicarÃ¡ nivel 2 de madurez Richardson (Uso correcto de verbos HTTP GET, POST, PUT, DELETE y CÃ³digos de Estado).
2. **Interno (Module-to-Module):** El estilo arquitectÃ³nico serÃ¡ **Event-Driven Architecture (Local)**.
   - Usaremos eventos de dominio publicables (ej. `Spring ApplicationEvents`). Un mÃ³dulo "publica" que algo pasÃ³ (`ReservaHardLockedEvent`), y el mÃ³dulo de Notificaciones actÃºa como "Observer" ciego escuchando ese evento.

### Consequences
- IntegraciÃ³n predecible, estÃ¡ndar de facto en la industria. Herramientas como Postman, Swagger/OpenAPI y Fetch API en React funcionan nativamente sin librerÃ­as pesadas (GraphQL Apollo, etc.).
- El desacoplamiento interno vÃ­a Eventos de Dominio asegura que si el servidor de correo se cae (SendGrid HTTP timeout), la transacciÃ³n del bloqueo en Base de datos no se haga Rollback por accidente.
# Deliverable 6 (D6): Communication Pattern Decision

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

## Architecture Decision Record (ADR-004)

### Context
La recepciÃ³n de webhooks de Wompi (Pasarela de Pagos) y el envÃ­o de cientos de correos de confirmaciÃ³n en paralelo puede consumir rÃ¡pidamente los hilos disponibles (Threads) del servidor web Tomcat subyacente de Spring Boot (Thread Starvation).

### Decision
- **SÃ­ncrono B2C:** Las peticiones del turista y del propietario (Auth, BÃºsquedas, CreaciÃ³n de Fincas, Intentos de Soft-Lock) serÃ¡n **SÃ­ncronas** bloqueantes. El usuario esperarÃ¡ en su pantalla (Spinner) hasta que la DB responda (Latencia proyectada < 300ms).
- **AsÃ­ncrono SistÃ©mico:** Las tareas pesadas y de 3eros serÃ¡n enviadas a una **Cola de Tareas en Background** asÃ­ncrona.
  - Al recibir el Webhook de Wompi, el backend muta el estado a Hard-Lock y *encola* un Job de "Enviar correo de confirmaciÃ³n". El worker de fondo (mismo monolito, diferente pool de hilos) procesa la cola sin afectar la respuesta HTTP de 200 OK a Wompi.
  - El purgado de Soft-Locks expirados no serÃ¡ *On-Demand* sÃ­ncrono, sino AsÃ­ncrono guiado por un Cron-Job cada 5 minutos.

### Consequences
- Garantiza cumplimiento del Escenario 2 del D1 (Resiliencia del Webhook).
- A futuro, requerirÃ¡ persistir la cola de mensajes si el volumen aumenta, insertando RabbitMQ o Redis (BullMQ/Spring Quartz) en la topologÃ­a (D3). Por ahora (Fase inicial), la cola puede gestionarse en la misma base de datos relacional (PatrÃ³n *Outbox*) o en memoria con polÃ­ticas estrictas de reintentos.
# Deliverable 7 (D7): Cross-Cutting Concerns

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

## 1. Security Strategy
- **Authentication:** MÃ³dulo M-01 delegarÃ¡ el cifrado de contraseÃ±as a **Bcrypt**. Los tokens de sesiÃ³n serÃ¡n JWT firmados asimÃ©tricamente (RS256) emitidos por el backend.
- **Authorization:** VerificaciÃ³n de roles (RBAC) inyectada a nivel de Filtro/Interceptor en Spring Boot y middlewares en Next.js (Edge middleware).
- **Data Privacy:** Todas las peticiones HTTP estarÃ¡n forzadas a HTTPS (TLS 1.3) vÃ­a CloudFront y Vercel Edge Network. Las variables sensibles (ej. `WOMPI_EVENT_SECRET`) jamÃ¡s se inyectarÃ¡n en el cÃ³digo, solo en las Variables de Entorno del servidor (`.env` / AWS Parameter Store).

## 2. Observability & Logging
- **Application Logs:** El monolito de Spring Boot utilizarÃ¡ SLF4J + Logback para imprimir JSON logs por `stdout`. En la infraestructura AWS ECS/Elastic Beanstalk, estos logs viajarÃ¡n automÃ¡ticamente a Amazon CloudWatch.
- **Traceability:** Cada peticiÃ³n HTTP entrante se le inyectarÃ¡ un `X-Request-ID` para trazarla a travÃ©s de los logs si falla.

## 3. Error Handling
- **Backend API:** CentralizaciÃ³n de excepciones vÃ­a `@ControllerAdvice` (Spring). Todo error arrojarÃ¡ un payload estÃ¡ndar estandarizado (RFC 7807 Problem Details for HTTP APIs).
  ```json
  {
    "type": "https://api.nosfuimosdefinca.com/errors/conflict",
    "title": "Double Booking Detected",
    "status": 409,
    "detail": "Fechas ya ocupadas"
  }
  ```
- **Frontend UI:** Uso de "Error Boundaries" en React para evitar que la app entera crashee si un solo componente falla.
# Deliverable 8 (D8): CI-CD & Environments Strategy

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

## 1. Environments Topology
El ecosistema completo (`Front + Back + DB`) existirÃ¡ en tres ambientes aislados:
- **`dev`:** Ambiente para uso diario de los desarrolladores. Conectado a servicios de Sandbox (ej. Wompi Sandbox). Base de datos efÃ­mera.
- **`staging`:** Espejo del ambiente de producciÃ³n. Usado para pruebas QA, UAT y validaciÃ³n final antes del lanzamiento.
- **`production`:** Entorno vivo con usuarios reales, conectado a pasarelas reales y con polÃ­ticas estrictas de copias de seguridad de BD.

## 2. Source Control Strategy (GitFlow Simplificado)
- **Ramas Principales:**
  - `main` (Despliega automÃ¡ticamente a `production`).
  - `develop` (Despliega automÃ¡ticamente a `staging`).
- **Feature Branches:** Todo desarrollo (ej. `feature/m-03-reservas`) nace de `develop` y se mezcla vÃ­a *Pull Request (PR)*. Solo se aprueba si las Pruebas Unitarias pasan.

## 3. CI/CD Pipeline (GitHub Actions & Vercel)
- **Frontend (Vercel):** *Zero Config.* Vercel escucha el repositorio de GitHub. Cualquier push a `main` hace deploy a producciÃ³n. Cualquier push a un PR genera una URL de previsualizaciÃ³n (Preview Environment).
- **Backend (GitHub Actions -> AWS):**
  - **CI (Continuous Integration):** Al empujar cÃ³digo, GitHub Actions corre el `./mvnw test`, compila el proyecto y reporta cobertura.
  - **CD (Continuous Deployment):** Si el *push* fue a la rama `main`, empaqueta el artefacto en un contenedor Docker, lo sube a AWS ECR y actualiza el cluster de ECS/Elastic Beanstalk sin *downtime* (Rolling Update).
# Deliverable 9 (D9): Component Diagram (C4 Model)

**Project:** Nos Fuimos de Finca
**Phase:** 5 â€” Architectural Design
**Status:** Approved

### 2. Level 1: System Context Diagram

```mermaid
C4Context
    title System Context - Nos Fuimos de Finca

    Person(turista, "Turista", "Usuario buscando y reservando fincas rurales.")
    Person(propietario, "Propietario", "DueÃ±o publicando su finca y gestionando inventario.")
    Person(admin, "Administrador", "Personal de soporte tÃ©cnico y moderaciÃ³n.")

    System(nosfuimos, "Nos Fuimos de Finca", "Plataforma central de reservas y catÃ¡logo de fincas.")

    System_Ext(wompi, "Wompi Pasarela", "Procesa tarjetas de crÃ©dito, dÃ©bito y PSE.")
    System_Ext(sendgrid, "SendGrid", "EnvÃ­o de correos transaccionales (Vouchers).")

    Rel(turista, nosfuimos, "Busca, reserva y paga")
    Rel(propietario, nosfuimos, "Publica y gestiona")
    Rel(admin, nosfuimos, "Modera contenido")

    Rel(nosfuimos, wompi, "Redirige para pago y recibe Webhooks")
    Rel(nosfuimos, sendgrid, "Ordena envÃ­o de emails")
```

### 3. Level 2: Container Diagram

```mermaid
C4Container
    title Container Diagram - Nos Fuimos de Finca

    Person(turista, "Turista", "Navegador Web/MÃ³vil")
    
    System_Boundary(c1, "Nos Fuimos de Finca") {
        Container(spa, "Single Page App (SSR)", "Next.js / React", "Provee las vistas B2C y B2B al navegador vÃ­a Vercel.")
        Container(api, "API Monolithic Backend", "Java / Spring Boot", "Orquesta la lÃ³gica de negocio modular. Corre en AWS.")
        ContainerDb(db, "Relational Database", "Amazon RDS PostgreSQL", "Almacena los datos estructurados. Maneja concurrencia transaccional.")
        ContainerDb(storage, "Object Storage", "Amazon S3", "Almacena imÃ¡genes crudas de las fincas.")
    }
    
    System_Ext(wompi, "Wompi")
    
    Rel(turista, spa, "Navega y clickea", "HTTPS")
    Rel(spa, api, "Consume Endpoints REST", "JSON/HTTPS")
    Rel(api, db, "Escribe y Lee datos", "JDBC/SSL")
    Rel(api, storage, "Sube fotos", "AWS SDK")
    
    Rel(spa, wompi, "Drop-in Widget checkout", "HTTPS")
    Rel(wompi, api, "EnvÃ­a Webhook de resultado", "JSON/HTTPS")
```
