# Entregable 6 (D6): Acceso a Datos y Repositorios

**Proyecto:** Nos Fuimos de Finca
**Fase:** 6 — Diseño Técnico
**Módulo:** MOD-REV
**Estado:** Aprobado

## 2. Interfaces de Repositorio y Consultas Complejas
```java
public interface ReviewRepository extends JpaRepository<Review, UUID> {
    List<Review> findByFincaIdAndStatus(UUID fincaId, String status);
}
```

## 3. Definiciones de DTO
```java
public class CreateReviewDTO {
    @NotNull public UUID fincaId;
    @Min(1) @Max(5) public int rating;
    public String comment;
}
```
