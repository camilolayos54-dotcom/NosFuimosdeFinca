# Entregable 7 (D7): Contratos API (OpenAPI)

**Proyecto:** Nos Fuimos de Finca
**Fase:** 6 — Diseño Técnico
**Módulo:** MOD-AUTH
**Estado:** Aprobado

## 2. Especificación OpenAPI YAML
```yaml
openapi: 3.0.3
info:
  title: Nos Fuimos de Finca - Auth API
  version: 1.0.0
paths:
  /api/v1/auth/login:
    post:
      summary: Autenticar usuario y obtener JWT
      tags: [Auth]
      requestBody:
        content:
          application/json:
             schema:
               $ref: '#/components/schemas/AuthDTO'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
```
