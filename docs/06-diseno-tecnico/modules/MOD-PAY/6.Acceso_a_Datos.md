# Entregable 6 (D6): Acceso a Datos y Repositorios

**Proyecto:** Nos Fuimos de Finca
**Fase:** 6 — Diseño Técnico
**Módulo:** MOD-PAY
**Estado:** Aprobado

## 2. Interfaces de Repositorio y Consultas Complejas

```java
public interface PaymentRepository extends JpaRepository<Payment, UUID> {
    
    // Consulta por transacción de Wompi para evitar procesar el mismo webhook 2 veces (Idempotencia)
    Optional<Payment> findByWompiTransactionId(String transactionId);

    // Búsqueda por Booking asociado
    Optional<Payment> findByBookingId(UUID bookingId);
}
```

## 3. Definiciones de DTO

```java
public class WebhookPayloadDTO {
    public EventData data;
    public Signature signature;
    
    public static class EventData {
        public Transaction transaction;
    }
    
    public static class Transaction {
        public String id;
        public String status;
        public int amount_in_cents;
        public String reference; // UUID de nuestro Booking
    }
}
```
