# Entregable 5 (D5): Diagramas de Clases y Entidades

**Proyecto:** Nos Fuimos de Finca
**Fase:** 6 — Diseño Técnico
**Módulo:** MOD-PAY
**Estado:** Aprobado

## 2. Diagrama de Clases UML

```mermaid
classDiagram
    class Payment {
        -UUID id
        -UUID bookingId
        -String wompiTransactionId
        -BigDecimal amount
        -PaymentStatus status
        -Date processedAt
        +approve(transactionId: String) void
        +decline(reason: String) void
    }
    
    class PaymentStatus {
        <<enumeration>>
        PENDING
        APPROVED
        DECLINED
        ERROR
    }
    
    class PaymentService {
        -PaymentRepository paymentRepo
        -BookingClient bookingClient
        -WompiSignatureValidator validator
        +initializePayment(bookingId: UUID, amount: BigDecimal) PaymentDTO
        +processWompiWebhook(payload: WebhookPayload) void
    }
    
    Payment *-- PaymentStatus
    PaymentService --> Payment
```
