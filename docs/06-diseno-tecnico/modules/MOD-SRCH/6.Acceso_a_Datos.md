# Entregable 6 (D6): Acceso a Datos y Repositorios

**Proyecto:** Nos Fuimos de Finca
**Fase:** 6 — Diseño Técnico
**Módulo:** MOD-SRCH
**Estado:** Aprobado

## 2. Interfaces de Repositorio y Consultas Complejas
```java
public interface FincaRepository extends JpaRepository<Finca, UUID> {
    // Búsqueda base con filtros
    @Query("SELECT f FROM Finca f WHERE f.status = 'APPROVED' AND f.locationCity = :city AND f.maxGuests >= :guests")
    List<Finca> findBySearchCriteria(String city, int guests);
}
```

## 3. Definiciones de DTO
```java
public class FincaSearchResponseDTO {
    public UUID id;
    public String title;
    public String city;
    public BigDecimal pricePerNight;
    public String primaryImageUrl;
}
```
