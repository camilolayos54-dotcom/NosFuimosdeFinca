# Entregable 6 (D6): Acceso a Datos y Repositorios

**Proyecto:** Nos Fuimos de Finca
**Fase:** 6 — Diseño Técnico
**Módulo:** MOD-PADM
**Estado:** Aprobado

## 2. Interfaces de Repositorio y Consultas Complejas
```java
public interface AdminFincaRepository extends Repository<Finca, UUID> {
    // Query para traer Fincas Pendientes de Aprobación
    @Query("SELECT f FROM Finca f WHERE f.status = 'PENDING'")
    List<Finca> findPendingFincas();
}
```

## 3. Definiciones de DTO
```java
public class AdminFincaActionDTO {
    @NotNull public UUID fincaId;
    @NotNull public String action; // APPROVE or REJECT
    public String rejectionReason;
}
```
