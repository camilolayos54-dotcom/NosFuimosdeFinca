# Entregable 7 (D7): Diagramas de Secuencia del Sistema (MOD-PADM)

**Proyecto:** Nos Fuimos de Finca
**Fase:** 4 — Modelado del Sistema
**Módulo:** MOD-PADM (Panel del Administrador)
**Estado:** Aprobado

### 1. SSD: Cambio de Estado de Finca (Aprobación)

```mermaid
sequenceDiagram
    autonumber
    actor Admin
    participant API
    participant DB
    participant EventQueue

    Admin->>API: PATCH /api/v1/admin/fincas/:id/status { status: 'aprobada' }
    API->>API: Verify Role == sys-admin
    API->>DB: UPDATE finca SET estado='aprobada' WHERE id=?
    DB-->>API: Affected rows: 1
    
    API->>EventQueue: Publish Event (FINCA_APROBADA, fincaId)
    API-->>Admin: 200 OK
```
