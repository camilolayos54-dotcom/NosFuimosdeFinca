# Entregable 7 (D7): Diagramas de Secuencia del Sistema (MOD-POWN)

**Proyecto:** Nos Fuimos de Finca
**Fase:** 4 — Modelado del Sistema
**Módulo:** MOD-POWN (Panel del Propietario)
**Estado:** Aprobado

### 1. SSD: Dashboard Data Aggregation

```mermaid
sequenceDiagram
    autonumber
    actor Propietario
    participant Web
    participant API
    participant DB

    Propietario->>Web: Carga Dashboard
    Web->>API: GET /api/v1/propietarios/me/dashboard
    API->>API: Extraer Propietario_ID de JWT
    
    API->>DB: Query Aggregation (Suma ingresos, count reservas) WHERE prop_id = ?
    DB-->>API: Row(TotalIngresos, ReservasNuevas)
    
    API-->>Web: 200 OK (Dashboard DTO)
    Web-->>Propietario: Render de gráficos y métricas
```
