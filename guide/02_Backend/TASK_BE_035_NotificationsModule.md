# TASK BE-035 — Notifications Module
**Module:** backend/.../notifications/
**Priority:** HIGH
**Depends On:** BE-010

## 1. Purpose
Notification.java entity. NotificationType enum (EMAIL, WHATSAPP, PUSH, IN_APP). NotificationService (orchestrates by type). WhatsAppService (WhatsApp Business API REST). EmailService (JavaMailSender SMTP). NotificationRepository.

## 2. Implementation Instructions
1. Create all notification module files.
